module.exports = {
  project: {
    android: {
      sourceDir: './android',
      appName: 'app',
      packageName: 'au.com.snughealth.apps.securephotoshare',
    },
  },
  // Support fonts placed either at the repo root `assets/fonts` or inside the `app` folder.
  assets: ['./assets/fonts'],
};
